# Monium — environment variables (TEMPLATE)
#
# This is a no-build static site, so the browser cannot read .env directly.
# The values that the app actually uses at runtime live in  js/config.js
# (copied from js/config.example.js, and gitignored).
#
# This .env.example exists for documentation and for when you add a build
# tool later (e.g. Vite, which would read VITE_SUPABASE_URL from a real .env).
#
# Find these in your Supabase dashboard → Settings → API.

NEXT_PUBLIC_SUPABASE_URL=https://kjzvpypzvzqprhajszhk.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_gXVVviAoBhZHFksC4AZCVg_1q6oGNbm